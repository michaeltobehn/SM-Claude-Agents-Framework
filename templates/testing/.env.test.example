# BMAD Lite – Test Environment
# ==============================
# Kopiere diese Datei nach .env.test und passe die Werte an.
#
# WICHTIG: .env.test NICHT in Git committen!
# Füge zu .gitignore hinzu: .env.test

# Base URL der App (lokal oder deployed)
BASE_URL=http://localhost:3000

# Test-User Credentials
# → Muss ein echter User in deiner Supabase Auth sein
# → Am besten: Eigenen Test-User anlegen (nicht deinen Admin-Account!)
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=changeme

# ══════════════════════════════════════════════════
# Supabase (PFLICHT für RLS-Tests und API-Tests)
# ══════════════════════════════════════════════════
# Diese Vars werden von Vitest API-Tests benötigt,
# insbesondere für RLS Policy Integration Tests.
#
# ⚠️ IMMER den ANON KEY verwenden!
# ❌ NIEMALS den Service Role Key (umgeht RLS!)
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_ANON_KEY=eyJ...
